import 'package:flutter/material.dart';

class MeetOurPeopleScreen extends StatelessWidget {
  const MeetOurPeopleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Meet Our People'),
      ),
      body: const Center(
        child: Text('Meet Our People'),
      ),
    );
  }
}
