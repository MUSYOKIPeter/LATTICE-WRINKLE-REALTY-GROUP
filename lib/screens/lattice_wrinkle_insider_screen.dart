import 'package:flutter/material.dart';

class LatticeWrinkleInsiderScreen extends StatelessWidget {
  const LatticeWrinkleInsiderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lattice Wrinkle Insider'),
      ),
      body: const Center(
        child: Text('Lattice Wrinkle Insider'),
      ),
    );
  }
}
